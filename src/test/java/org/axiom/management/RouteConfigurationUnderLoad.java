package org.axiom.management;

import static org.junit.Assert.assertThat;
import static org.hamcrest.CoreMatchers.not;

//@RunWith(Theories.class)
public class /*Test*/RouteConfigurationUnderLoad {

    /*private static ApplicationContext springContext;
    private static CamelContext camelContext;

    @BeforeClass
    public static void beforeAll() {
        springContext = new ClassPathXmlApplicationContext("classpath:/server-context.xml");
        camelContext = (CamelContext) springContext.getBean("camel");
    }

    @DataPoint public static String GOOD_USERNAME = "optimus";
    @DataPoint public static String USERNAME_WITH_SLASH = "optimus/prime";

    @Theory
    public void addingRoutesWhilstConsumingMessagesAsynchrouslyDoesNotBreakBasedOnTiming(String username) {
        assumeThat(username, not(containsString("/")));
        assertThat(username, containsString(username));
    }*/

}


